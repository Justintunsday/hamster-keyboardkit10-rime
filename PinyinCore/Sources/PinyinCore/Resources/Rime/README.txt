CI populates this directory with the pinned rime-ice 2026.06.30 full resource set.
The checked-in placeholder keeps the Swift package layout valid on hosts without
the resource archive. The runtime falls back to LocalPinyinEngine when required
RIME files are absent.
