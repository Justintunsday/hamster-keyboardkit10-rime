# 诊断日志

在主 App 的“输入法设置 → 诊断日志”中可以查看、刷新、复制、分享或清除主 App 与键盘扩展的共享诊断记录。

记录保存在 App Group `group.dev.fuxiao.app.Hamster` 的 `Diagnostics/` 目录。主 App 与每个键盘 Bundle 各自写入一个经过 Bundle ID 清理的 `events-*.json` 文件，并使用原子替换避免跨进程互相覆盖；主 App 查看时会合并所有可识别文件，单个文件最多 200 条事件且不超过 64 KiB，总目录保留上限为 256 KiB。损坏的单个文件会被跳过。写入在后台串行队列中进行，App Group 不可用或写入失败不会影响输入法运行。

事件只包含时间、进程/目标、严重级别、固定类别和生命周期消息，以及 App 版本、构建号、系统版本和进程号。实现不会记录键入字符、拼音组合、候选词、周边文档、凭据或其他用户内容。键盘和主 App 启动时会留下会话标记；下一次启动若标记仍存在，会增加“上次会话未正常结束”提示。iOS 可能直接终止进程，因此这只是尽力而为的线索，不是完整的崩溃报告，也不安装信号处理器、异常处理器或 stderr 重定向。

Windows 环境无法运行 Xcode 或 iOS 模拟器。请在 macOS 上用项目的 GitHub Actions 或 Xcode 构建验证，并在复现问题后从上述页面分享日志，同时附上系统生成的完整崩溃报告（若有）。